# Ralph Progress Log
Started: Fri Mar 06 2026

## Codebase Patterns
- **Client component tests**: Use `@testing-library/react` + `userEvent.setup()` for interactions
- **Email inputs**: `type="email"` inputs have no label-input `htmlFor`/`id` association; query with `document.querySelector('input[type="email"]')` instead of `getByRole("textbox", { name: /email/i })`
- **Debounce testing**: Use `vi.useFakeTimers({ shouldAdvanceTime: true })` + `userEvent.setup({ advanceTimers: vi.advanceTimersByTime })`, then `vi.advanceTimersByTime(delay)` wrapped in `act()`
- **Fetch mocking**: `vi.stubGlobal("fetch", mockFetch)` with `mockFetch = vi.fn()` in `beforeEach`
- **Pre-existing build failure**: `e2e/helpers/db.ts:152` has type error (`string | undefined` not assignable to `string`) — not caused by unit tests
- **Pre-existing lint errors**: 818 errors exist in codebase; new test files should lint clean individually with `npx eslint <file>`
- **AddUserModal props**: `{ user, regions, onClose, onSave }` — NOT `{ isOpen, onClose, onSave, editUser, currentUserEmail }` as PRD stated
- **Multiple selects**: Use `screen.getAllByRole("combobox")` and index to target specific `<select>` elements
- **PRD props often wrong**: Always read actual source component to get real props interface before writing tests
- **Duplicate text in table+modal**: When a component shows the same text in a table row AND a modal, use `getAllByText()` or scope queries with `within()` to avoid `getByText` failing on multiple matches
- **Stats card counts**: Use `within(card)` scoping to check stat values, since count numbers like "1" or "2" may appear multiple times on page
- **Mock child components**: Use `vi.mock("./ChildComponent", ...)` with stub that exposes `data-testid` + callback buttons to test parent integration cleanly
- **Window dialogs**: `vi.stubGlobal("alert", vi.fn())` + `vi.stubGlobal("confirm", vi.fn(() => true))` in `beforeEach` for components using `alert()`/`confirm()`
- **Date inputs without htmlFor**: `type="date"` inputs may lack `htmlFor`/`id` association; query with `document.querySelector('input[type="date"]')` instead of `getByLabelText("Date")`
- **Duplicate count badges**: When selecting topics changes a count badge and another chapter has the same count text, use `getAllByText()` and check `className` to distinguish blue (selected) vs gray (total) badges
- **React 19 use(params) in client components**: Wrap component in `<Suspense>` and use `await act(async () => { render(...) })` to let the Promise resolve before assertions
- **Server component test pattern**: Use `vi.hoisted()` for mock variables referenced in `vi.mock()` factories. Redirect sentinel: `mockRedirect = vi.fn((url) => { throw new Error('REDIRECT:' + url); })`. Test redirects: `await expect(Page()).rejects.toThrow("REDIRECT:/")`. Test rendering: `const jsx = await Page(); render(jsx);`. Mock `next/link` as `<a>` stub. Mock `next-auth`, `@/lib/auth`, `next/navigation`, and permission functions.
- **Top-level env vars**: Pages that use `const X = process.env.X` at module top-level capture env vars at import time. Since `vi.mock()` is hoisted above `process.env` assignments, use pattern-matching (`expect(url).toContain(...)`) for fetch URL assertions instead of exact URL matching.
- **resetAllMocks for server pages**: Use `vi.resetAllMocks()` (not `clearAllMocks`) in `beforeEach` for server component tests with `mockResolvedValueOnce()` — `clearAllMocks` does NOT clear pending once-queues, causing mock leakage. After `resetAllMocks`, re-establish sentinel implementations for redirect/notFound.
- **Recharts mock coverage**: Mock `Bar`/`Pie` to render `{children}` (not `() => null`) so `.map()` Cell callbacks execute. Mock `Tooltip`/`Pie` to invoke callback props (`formatter`, `label`) so those arrow functions get V8 coverage. Use typed interfaces (`type P = { children?: React.ReactNode }`) to avoid lint `any` errors.

- **Tailwind v4 @theme inline**: Register CSS custom properties in `@theme inline { }` block to generate utility classes (e.g., `--color-accent: var(--color-accent)` → enables `bg-accent`, `text-accent`, `border-accent`). Without registration, must use verbose `bg-[var(--color-accent)]` syntax.
- **Focus ring suppression**: Global CSS handles `outline: none` and `--tw-ring-shadow` reset for all interactive elements. Still need to remove `focus:ring-*` classes from JSX to avoid generating unused CSS.
- **Status badges**: Use `statusBadgeClass(status)` from `@/lib/visit-actions` — includes `rounded-full px-2.5 py-0.5 text-xs font-medium` + theme colors. Callers just need `inline-flex ${statusBadgeClass(status)}`.
- **Progressive padding**: `px-4 sm:px-6 md:px-16 lg:px-32 xl:px-64 2xl:px-96 py-6 md:py-8` — full-bleed page layout wrapper
- **Table headers (Ledger)**: `bg-bg-card-alt border-b-2 border-border-accent` with `text-xs font-bold text-text-muted uppercase tracking-wider`
- **Section headers (Ledger)**: `text-lg font-bold text-text-primary uppercase tracking-wide mb-4 border-b-2 border-border-accent pb-2`
- **Font-mono in text strings**: Don't wrap numbers in `<span>` inside getByText-tested strings — RTL can't match split text. Use `font-mono` on parent container instead.

- **AF team interaction validation**: `ValidationResult` is defined locally in `af-team-interaction.ts` (not shared with `classroom-observation-rubric.ts`) to avoid coupling
- **Lenient vs strict validation pattern**: `validateAFTeamInteractionSave()` (lenient) accepts `{}`, ignores unknown question keys; `validateAFTeamInteractionComplete()` (strict) requires ≥1 teacher + all 9 boolean answers
- **Individual teacher interaction validation**: Per-teacher validation with attendance gating — present teachers need all 13 questions answered (strict), absent/on_leave skip questions. `validateIndividualTeacherSave()` (lenient), `validateIndividualTeacherComplete()` (strict). Only allowed top-level key is `teachers`.
- **Adding action types atomically**: When adding to `ACTION_TYPES`, must also add to `ACTION_FORM_CONFIGS` (Record<ActionType, ...>) in same commit or TypeScript breaks. Also update ActionTypePickerModal test count.

---

## 2026-03-09 - US-001
Session: 0e788491-1c7e-43ed-b96a-327a42ce05af
- Implemented config, types, and validation foundation for individual_af_teacher_interaction
- Files changed:
  - `src/lib/visit-actions.ts` — added 10th action type
  - `src/lib/visit-actions.test.ts` — updated exhaustiveness test (9 -> 10)
  - `src/lib/individual-af-teacher-interaction.ts` — NEW: 5 sections, 13 questions, attendance gating, lenient/strict validation
  - `src/components/visits/ActionDetailForm.tsx` — added ACTION_FORM_CONFIGS entry
  - `src/components/visits/ActionTypePickerModal.test.tsx` — updated disabled count (7 -> 8)
- **Learnings for future iterations:**
  - Adding a new action type also breaks the ActionTypePickerModal test that counts disabled types — update it too
  - Individual teacher validation pattern: `teachers` is the only top-level key (vs AF team which has `teachers` + `questions`)
  - Per-teacher questions validation uses attendance gating: strict mode only requires answers for `present` teachers
  - 1230 tests passing after this change (was 1228 in CLAUDE.md — count grew from other work)
---

## 2026-03-09 - US-002
Session: dd99c1c1-0bcc-4e6f-9708-ca0ed97196cd
- Created comprehensive validation unit tests for individual-af-teacher-interaction
- Files changed:
  - `src/lib/individual-af-teacher-interaction.test.ts` — NEW: 29 tests (3 config + 18 lenient + 8 strict)
- **Learnings for future iterations:**
  - Individual teacher validation has attendance gating — strict mode only requires answers for `present` teachers, unlike AF team interaction which has flat question validation
  - `buildCompleteTeacher()` helper needs attendance parameter to conditionally populate questions (present fills all 13, on_leave/absent gets empty {})
  - 1259 tests passing across 79 files after this change
---

## 2026-03-09 - US-003
Session: e417cdc1-e67b-4813-b65d-d04121e23c73
- Added PATCH route validation for individual_af_teacher_interaction action type
- Files changed:
  - `src/app/api/pm/visits/[id]/actions/[actionId]/route.ts` — imported validation functions, added validation block after af_team_interaction block
- **Learnings for future iterations:**
  - Adding validation to PATCH route is trivial — just follow the existing af_team_interaction/classroom_observation pattern
  - Lenient for in_progress, strict for completed — same conditional pattern as other action types
  - 1259 tests still passing across 79 files (no new tests needed for this story)
---

## 2026-03-09 - US-004
Session: 01f1ce90-1ff6-4776-b2f5-dce8f1b75946
- Added END route validation for individual_af_teacher_interaction with all-teachers DB check
- Files changed:
  - `src/app/api/pm/visits/[id]/actions/[actionId]/end/route.ts` — imported `validateIndividualTeacherComplete`, added `individualTeacherValidationError()` (data shape check) and `allTeachersRecordedError()` (DB query to verify all school teachers recorded), wired both into pre-update and concurrent-fallback paths
- **Learnings for future iterations:**
  - END route has 2 validation paths (pre-update + concurrent fallback) — both must include new validations
  - `allTeachersRecordedError()` is async (DB query) unlike other validation functions — uses same SQL as `GET /api/pm/teachers` with TEXT[] array containment
  - `visit.school_code` and `visit.school_region` are both available from `loadVisitAccessTarget()` (LEFT JOIN on school table)
  - 1259 tests still passing across 79 files (no new tests needed for this story)
---

## 2026-03-09 - US-005
Session: a10be337-ec97-422e-8739-e5bf3bf16fff
- Added visit completion rule change: now requires all 3 action types (classroom_observation + af_team_interaction + individual_af_teacher_interaction)
- Files changed:
  - `src/app/api/pm/visits/[id]/complete/route.ts` — added sequential checks for completed af_team_interaction and individual_af_teacher_interaction after existing classroom_observation check
- **Learnings for future iterations:**
  - Checks are sequential and short-circuit on first missing type (classroom → AF team → individual teacher)
  - Used `LIMIT 1` since only existence check is needed (unlike classroom observation which validates rubric data)
  - 3 existing complete route tests now fail as expected — they need mocks for the 2 new queries (fixed in US-008)
  - The mock `query` returns `undefined` when mock queue is exhausted, causing `TypeError: Cannot read properties of undefined (reading 'length')` — this is expected
  - 1259 tests total, 3 expected failures in complete route (9 pass + 3 fail = 12 tests in file)
---

## 2026-03-09 - US-006
Session: dd99c1c1-0bcc-4e6f-9708-ca0ed97196cd
- Added 7 PATCH route unit tests for individual_af_teacher_interaction validation
- Files changed:
  - `src/app/api/pm/visits/[id]/actions/[actionId]/route.test.ts` — added `buildValidIndividualTeacherData()` helper, 7 new tests (3 lenient success, 2 lenient rejection, 1 strict rejection, 1 strict success)
- **Learnings for future iterations:**
  - Individual teacher PATCH tests follow exact same pattern as AF team interaction tests — set up action_type, mock DB returns, assert status codes
  - `buildValidIndividualTeacherData()` creates a single present teacher with all 13 questions answered — reusable for strict validation tests
  - 1266 total tests (1263 pass + 3 expected complete route failures from US-005)
---

## 2026-03-09 - US-007
Session: 4daebfc3-9ce9-44e9-ae3e-044c5f1cce6b
- Added 6 END route unit tests for individual_af_teacher_interaction validation including all-teachers DB check
- Files changed:
  - `src/app/api/pm/visits/[id]/actions/[actionId]/end/route.test.ts` — added `buildValidIndividualTeacherData()` helper, 6 new tests (incomplete data 422, null data 422, missing teachers 422, absent/on_leave success, full success, concurrent fallback validation)
- **Learnings for future iterations:**
  - END route for individual teacher has extra query mock needed: `allTeachersRecordedError()` queries `user_permission` table — mock must return teacher list before the UPDATE mock
  - Query flow for successful individual teacher END: visit → action → teacher list (allTeachersRecordedError) → UPDATE RETURNING
  - Concurrent fallback test: pre-update path passes all validations (valid data + all teachers), UPDATE returns 0 rows, re-fetch returns incomplete data → 422
  - 1272 total tests (1269 pass + 3 expected complete route failures from US-005)
---

## 2026-03-09 - US-008
Session: d26959fe-8253-48db-9ff5-0511ca25196a
- Updated COMPLETE route tests for 3-type requirement (classroom + AF team + individual teacher)
- Files changed:
  - `src/app/api/pm/visits/[id]/complete/route.test.ts` — updated 2 successful completion test mocks (PM + admin) to include AF team + individual teacher query mocks, replaced "supplementary" test with 4 new tests (missing AF team 422, missing individual teacher 422, all 3 types success, short-circuit order verification)
- **Learnings for future iterations:**
  - Successful completion now requires 6 query mocks: visit → in-progress check → classroom actions → AF team check → individual teacher check → UPDATE
  - Mock call index for UPDATE shifted from [3] to [5] due to 2 new queries
  - Short-circuit test verifies only 3 queries fired when classroom check fails (never reaches AF team/individual teacher)
  - 1275 tests passing across 79 files (all green, 3 previously broken tests now fixed)
---

## 2026-03-09 - US-009
Session: 3253f547-55fa-4d67-805f-181f54f7a949
- Enabled individual_af_teacher_interaction in the action type picker modal
- Files changed:
  - `src/components/visits/ActionTypePickerModal.tsx` — added `individual_af_teacher_interaction` to the enabled check (line 47)
  - `src/components/visits/ActionTypePickerModal.test.tsx` — added 2 new tests (radio selectable, submit calls onSubmit), updated disabled count 8→7 with 3 enabled types
- **Learnings for future iterations:**
  - Enabling a new action type in the picker is a one-line change + test updates
  - Test count now 1277 across 79 files
---

## 2026-03-09 - US-010
Session: df98f051-627f-4854-9541-9f7d93487498
- Created IndividualAFTeacherInteractionForm.tsx component (~310 lines)
- Files changed:
  - `src/components/visits/IndividualAFTeacherInteractionForm.tsx` — NEW: per-teacher collapsible accordion form with attendance gating, question sections, add/remove/progress
- **Learnings for future iterations:**
  - Component follows AFTeamInteractionForm pattern but with per-teacher nesting: each teacher gets their own collapsible section with attendance radios + questions
  - Attendance gating: when attendance changes to on_leave/absent, questions are cleared (`questions: {}`)
  - Add Teacher uses a `<select>` dropdown with `onChange` + reset pattern (not a separate button + dropdown) for simplicity
  - `expandedIds` state (Set<number>) tracks which teacher sections are open — multiple can be open simultaneously
  - `remainingTeachers` = fetched - recorded; when empty + all fetched are recorded, shows "All teachers recorded" message
  - Progress bar shows `Recorded: X/Y teachers` + attendance breakdown — always visible (unlike AF team form which gates on teacher selection)
  - Read-only mode: sections are collapsed by default, expandable to static text (no radios/textareas)
  - Remark reveal state uses `${teacherId}-${questionKey}` composite keys since remarks are per-teacher-per-question
  - 1277 tests still passing across 79 files (no test changes needed — component tests are in US-011)
---

## 2026-03-09 - US-011
Session: 509ad335-a883-425c-848d-d1ffbfafa4a0
- Created comprehensive unit tests for IndividualAFTeacherInteractionForm component (25 tests)
- Files changed:
  - `src/components/visits/IndividualAFTeacherInteractionForm.test.tsx` — NEW: 25 tests across 8 describe blocks (teacher fetch 3, add teacher + sections 6, collapsible behavior 4, editing + removing 5, progress bar 1, disabled/read-only 3, edge cases 2, remark interaction 1)
- **Learnings for future iterations:**
  - RTL `getByText("Present", { selector: "p span + *" })` doesn't work for sibling text nodes — use `getByText("Attendance:")` to verify the static label instead
  - `userEvent.selectOptions(select, "1")` works with `<select>` + `onChange` handler for the Add Teacher dropdown pattern
  - `buildTeacherEntry()` helper with attendance + answerAll params is reusable for both test setup and edge case verification
  - Read-only mode (`disabled=true`) renders `<p>` elements with "Yes"/"No" text instead of radio inputs — can verify count with `getAllByText("Yes")`
  - 1302 tests passing across 80 files after this change
---

## 2026-03-09 - US-012
Session: dd99c1c1-0bcc-4e6f-9708-ca0ed97196cd
- Wired IndividualAFTeacherInteractionForm into ActionDetailForm.tsx
- Files changed:
  - `src/components/visits/ActionDetailForm.tsx` — imported form component + config, added INDIVIDUAL_TEACHER_ACTION_TYPE constant, added to SAVE_BEFORE_END_TYPES, added sanitizeIndividualTeacherPayload() and bootstrapIndividualTeacherPayload(), updated normalizeFormDataForAction() and sanitizePatchData() with individual teacher branches, wired form in render chain between AF team and fallback, updated END 422 error to 3-way ternary with type-specific message
- **Learnings for future iterations:**
  - Wiring a new form type into ActionDetailForm follows a clear pattern: import + constant + SAVE_BEFORE_END_TYPES + sanitize/bootstrap + normalizeFormData + sanitizePatch + render branch + error message
  - sanitizeIndividualTeacherPayload strips unknown keys and only keeps known question keys from config — mirrors sanitizeAFTeamPayload pattern but with per-teacher nesting
  - bootstrapIndividualTeacherPayload returns `{ teachers: [] }` for null/undefined, same as AF team's `{ teachers: [], questions: {} }`
  - Individual teacher gets its own END 422 message: "Please complete all required fields and record all teachers before ending this interaction."
  - 1302 tests still passing across 80 files (no test changes needed — page tests are in US-014)
---

## 2026-03-09 - US-013
Session: 74f84d5c-2d1c-45c2-a963-a6953eefd38f
- Added action card stats for individual_af_teacher_interaction: interface, stats function, rendering, and tests
- Files changed:
  - `src/components/visits/ActionPointList.tsx` — imported config, added `IndividualTeacherInteractionStats` interface + `getIndividualTeacherInteractionStats()` function, added rendering block with `data-testid='individual-teacher-stats-{id}'`
  - `src/components/visits/ActionPointList.test.tsx` — added 8 new tests: 4 component rendering tests (stats with attendance breakdown, multiple present, empty/undefined, non-matching type) + 4 standalone stats unit tests (mixed attendance, undefined, empty array, no teachers key)
- **Learnings for future iterations:**
  - Individual teacher stats pattern is simpler than AF team (no question progress) — just count teachers by attendance type
  - Stats return null for empty teachers array (not just missing key) — unlike AF team which returns null only when both counts are 0
  - 1310 tests passing across 80 files after this change
---

## 2026-03-09 - US-014
Session: 112990fc-c05c-4622-8fad-46439bd741a1
- Added 5 action detail page tests for individual_af_teacher_interaction
- Files changed:
  - `src/app/visits/[id]/actions/[actionId]/page.test.tsx` — imported `INDIVIDUAL_AF_TEACHER_INTERACTION_CONFIG`, added 5 new tests: renderer loads with correct title + dual testid, null data bootstraps to `{ teachers: [] }`, auto-save before /end, save failure shows type-specific message, END 422 shows "record all teachers" guidance
- **Learnings for future iterations:**
  - Individual teacher interaction form component renders its own `data-testid="action-renderer-individual_af_teacher_interaction"` inside the form wrapper that also has the same testid — use `getAllByTestId().toHaveLength(2)` like AF team interaction
  - Save failure message for individual teacher uses the generic "Could not save form data" (same as AF team), but END 422 has unique message mentioning "record all teachers"
  - 1315 tests passing across 80 files after this change
---

## 2026-03-09 - US-015
Session: dd99c1c1-0bcc-4e6f-9708-ca0ed97196cd
- Added E2E test infrastructure: data builders and teacher seeding for individual teacher interaction
- Files changed:
  - `e2e/helpers/db.ts` — added `buildCompleteIndividualTeacherInteractionData()` (strict-valid payload builder with all 13 questions answered true), `seedIndividualTeacherTestTeachers()` (upserts 3 deterministic teacher user_permission rows, cleans up stray teacher rows while preserving AF team test teachers), imported `INDIVIDUAL_AF_TEACHER_INTERACTION_CONFIG`
  - `e2e/tests/visits.spec.ts` — added `fillIndividualTeacherInteractionForm()` helper (selects first teacher from dropdown, verifies present attendance, answers all 13 questions with Yes), imported new helpers and config
- **Learnings for future iterations:**
  - `seedIndividualTeacherTestTeachers()` cleanup query must preserve AF team test teacher emails to avoid breaking existing AF team E2E tests
  - `buildCompleteIndividualTeacherInteractionData()` accepts optional `teacherIds` array (like `buildCompleteClassroomObservationData` is static, but individual teacher needs to match actual DB teacher IDs)
  - `fillIndividualTeacherInteractionForm()` extracts teacher ID from the section's `data-testid` attribute since it's dynamically assigned from the DB
  - All pre-existing tsc errors are in test files (Vitest globals) — no errors in e2e/ files
  - 1315 tests still passing across 80 files (no new unit tests — this is E2E infrastructure only)
---

## 2026-03-09 - US-016
Session: 5086446e-62c7-4f1b-ad9f-d4cea360a1f0
- Added 5 new E2E tests + updated 5 existing E2E tests for individual teacher interaction
- Files changed:
  - `e2e/tests/visits.spec.ts` — added `seededTeachers` variable, `fillAllIndividualTeachers()` helper, 5 new tests (PM full lifecycle, END blocked missing teachers, absent/on_leave pass, visit completion requires 3 types, program admin read-only), updated `beforeAll` to seed individual teacher test teachers, updated 5 existing tests to seed all 3 action types for completion (`complete-blocked-without-rubric`, `complete-visit-success`, `moderate-gps-warning`, `admin-can-complete`, renamed `visit-completes-with-both` → `visit-completes-with-all-three-required-action-types`)
- **Learnings for future iterations:**
  - `as const` assertion cannot be applied to ternary expressions — use `const att: string = ...` or remove `as const`
  - `fillAllIndividualTeachers()` pattern: count options at start, iterate with `selectOption({ index: 1 })` (always first non-disabled option), first teacher gets present+all questions, rest get absent attendance
  - For tests that try to complete a visit successfully, ALL must seed 3 completed action types (classroom + AF team + individual teacher) — easy to miss existing tests when adding new completion requirements
  - `page.request.get()` inherits browser context cookies — can call authenticated APIs directly without navigating
  - The `allTeachersRecordedError()` in END route uses same SQL as `GET /api/pm/teachers` — both check school_codes, regions, and level=3 teachers. E2E tests must account for all teacher sources, not just seeded ones
  - E2E tests for END validation: seed data via DB, navigate to action page, click End Action — the save-before-end pattern PATCHes current form data then POSTs /end, so seeded data is what gets validated
  - Program admin read-only for individual teacher uses completely different render path (static text, no radios) unlike AF team interaction (disabled radios) — verify `input[type="radio"]` count is 0, not just disabled
  - 1315 unit tests still passing (no new unit tests in this story — E2E only)
---
