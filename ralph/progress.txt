# Ralph Progress Log
Started: Wed Mar 18 2026

## Codebase Patterns
- **Client component tests**: Use `@testing-library/react` + `userEvent.setup()` for interactions
- **Email inputs**: `type="email"` inputs have no label-input `htmlFor`/`id` association; query with `document.querySelector('input[type="email"]')` instead of `getByRole("textbox", { name: /email/i })`
- **Debounce testing**: Use `vi.useFakeTimers({ shouldAdvanceTime: true })` + `userEvent.setup({ advanceTimers: vi.advanceTimersByTime })`, then `vi.advanceTimersByTime(delay)` wrapped in `act()`
- **Fetch mocking**: `vi.stubGlobal("fetch", mockFetch)` with `mockFetch = vi.fn()` in `beforeEach`
- **Pre-existing build failure**: `e2e/helpers/db.ts:152` has type error (`string | undefined` not assignable to `string`) — not caused by unit tests
- **Pre-existing lint errors**: 818 errors exist in codebase; new test files should lint clean individually with `npx eslint <file>`
- **AddUserModal props**: `{ user, regions, onClose, onSave }` — NOT `{ isOpen, onClose, onSave, editUser, currentUserEmail }` as PRD stated
- **Multiple selects**: Use `screen.getAllByRole("combobox")` and index to target specific `<select>` elements
- **PRD props often wrong**: Always read actual source component to get real props interface before writing tests
- **Duplicate text in table+modal**: When a component shows the same text in a table row AND a modal, use `getAllByText()` or scope queries with `within()` to avoid `getByText` failing on multiple matches
- **Stats card counts**: Use `within(card)` scoping to check stat values, since count numbers like "1" or "2" may appear multiple times on page
- **Mock child components**: Use `vi.mock("./ChildComponent", ...)` with stub that exposes `data-testid` + callback buttons to test parent integration cleanly
- **Window dialogs**: `vi.stubGlobal("alert", vi.fn())` + `vi.stubGlobal("confirm", vi.fn(() => true))` in `beforeEach` for components using `alert()`/`confirm()`
- **Date inputs without htmlFor**: `type="date"` inputs may lack `htmlFor`/`id` association; query with `document.querySelector('input[type="date"]')` instead of `getByLabelText("Date")`
- **Duplicate count badges**: When selecting topics changes a count badge and another chapter has the same count text, use `getAllByText()` and check `className` to distinguish blue (selected) vs gray (total) badges
- **React 19 use(params) in client components**: Wrap component in `<Suspense>` and use `await act(async () => { render(...) })` to let the Promise resolve before assertions
- **Server component test pattern**: Use `vi.hoisted()` for mock variables referenced in `vi.mock()` factories. Redirect sentinel: `mockRedirect = vi.fn((url) => { throw new Error('REDIRECT:' + url); })`. Test redirects: `await expect(Page()).rejects.toThrow("REDIRECT:/")`. Test rendering: `const jsx = await Page(); render(jsx);`. Mock `next/link` as `<a>` stub. Mock `next-auth`, `@/lib/auth`, `next/navigation`, and permission functions.
- **Top-level env vars**: Pages that use `const X = process.env.X` at module top-level capture env vars at import time. Since `vi.mock()` is hoisted above `process.env` assignments, use pattern-matching (`expect(url).toContain(...)`) for fetch URL assertions instead of exact URL matching.
- **resetAllMocks for server pages**: Use `vi.resetAllMocks()` (not `clearAllMocks`) in `beforeEach` for server component tests with `mockResolvedValueOnce()` — `clearAllMocks` does NOT clear pending once-queues, causing mock leakage. After `resetAllMocks`, re-establish sentinel implementations for redirect/notFound.
- **Recharts mock coverage**: Mock `Bar`/`Pie` to render `{children}` (not `() => null`) so `.map()` Cell callbacks execute. Mock `Tooltip`/`Pie` to invoke callback props (`formatter`, `label`) so those arrow functions get V8 coverage. Use typed interfaces (`type P = { children?: React.ReactNode }`) to avoid lint `any` errors.

- **Tailwind v4 @theme inline**: Register CSS custom properties in `@theme inline { }` block to generate utility classes (e.g., `--color-accent: var(--color-accent)` → enables `bg-accent`, `text-accent`, `border-accent`). Without registration, must use verbose `bg-[var(--color-accent)]` syntax.
- **Focus ring suppression**: Global CSS handles `outline: none` and `--tw-ring-shadow` reset for all interactive elements. Still need to remove `focus:ring-*` classes from JSX to avoid generating unused CSS.
- **Status badges**: Use `statusBadgeClass(status)` from `@/lib/visit-actions` — includes `rounded-full px-2.5 py-0.5 text-xs font-medium` + theme colors. Callers just need `inline-flex ${statusBadgeClass(status)}`.
- **Progressive padding**: `px-4 sm:px-6 md:px-16 lg:px-32 xl:px-64 2xl:px-96 py-6 md:py-8` — full-bleed page layout wrapper
- **Table headers (Ledger)**: `bg-bg-card-alt border-b-2 border-border-accent` with `text-xs font-bold text-text-muted uppercase tracking-wider`
- **Section headers (Ledger)**: `text-lg font-bold text-text-primary uppercase tracking-wide mb-4 border-b-2 border-border-accent pb-2`
- **Font-mono in text strings**: Don't wrap numbers in `<span>` inside getByText-tested strings — RTL can't match split text. Use `font-mono` on parent container instead.

- **AF team interaction validation**: `ValidationResult` is defined locally in `af-team-interaction.ts` (not shared with `classroom-observation-rubric.ts`) to avoid coupling
- **Lenient vs strict validation pattern**: `validateAFTeamInteractionSave()` (lenient) accepts `{}`, ignores unknown question keys; `validateAFTeamInteractionComplete()` (strict) requires ≥1 teacher + all 9 boolean answers
- **Individual teacher interaction validation**: Per-teacher validation with attendance gating — present teachers need all 13 questions answered (strict), absent/on_leave skip questions. `validateIndividualTeacherSave()` (lenient), `validateIndividualTeacherComplete()` (strict). Only allowed top-level key is `teachers`.
- **Adding action types atomically**: When adding to `ACTION_TYPES`, must also add to `ACTION_FORM_CONFIGS` (Record<ActionType, ...>) in same commit or TypeScript breaks. Also update ActionTypePickerModal test count.

---

## 2026-03-18 13:51 - US-001
Session: b88214e0-09ee-4471-8e9c-7a8482d300d1
- Created `src/lib/group-student-discussion.ts` with config, types, VALID_GRADES, and both validation functions
- Files changed: `src/lib/group-student-discussion.ts` (new, 186 lines)
- All 1402 existing tests pass (no regressions)
- **Learnings for future iterations:**
  - Followed `src/lib/principal-interaction.ts` pattern exactly — same interfaces, validateQuestions helper, isPlainObject, lenient/strict split
  - Added `isValidGrade()` helper for grade validation (checks typeof number, isInteger, and inclusion in VALID_GRADES)
  - Grade validation in lenient mode only triggers if `grade` key is present; strict mode always requires it
  - ALLOWED_TOP_LEVEL_KEYS includes both "grade" and "questions" (principal interaction only has "questions")
---

## 2026-03-18 13:54 - US-002
Session: 30b369dc-8852-466d-8417-789bf52edf2d
- Created `src/lib/group-student-discussion.test.ts` with 36 tests following principal-interaction.test.ts pattern
- Files changed: `src/lib/group-student-discussion.test.ts` (new, 290 lines)
- All 1438 tests pass across 84 files (no regressions)
- **Learnings for future iterations:**
  - Followed `principal-interaction.test.ts` pattern closely, added grade-specific tests (valid 11/12, invalid 10/13/"eleven"/null/11.5)
  - Config integrity tests verify: section count/titles, question count/uniqueness, key ordering, gc_* prefix format
  - Complete validation tests must include grade in payload (unlike principal interaction which has no grade)
  - buildCompletePayload() helper takes optional grade param for testing both grade 11 and 12 paths
---

## 2026-03-18 13:56 - US-003
Session: 30b369dc-8852-466d-8417-789bf52edf2d
- Created `src/lib/individual-student-discussion.ts` with config, types, VALID_GRADES, and both validation functions
- Files changed: `src/lib/individual-student-discussion.ts` (new, 237 lines)
- All 1438 existing tests pass (no regressions)
- **Learnings for future iterations:**
  - Followed `src/lib/individual-af-teacher-interaction.ts` pattern closely — removed attendance gating, added grade per student
  - Key structural difference from teacher interaction: no `attendance` field, no attendance-gated question validation
  - `validateStudentEntries()` validates id (positive int, unique), name (non-empty string), grade (11/12 via isValidGrade), and questions leniently or strictly
  - In lenient mode, name validation only triggers if name is present (not undefined); in strict mode name is always required
  - Grade validation in lenient mode only triggers if grade key is present; strict mode always requires it
  - ALLOWED_TOP_LEVEL_KEYS is just `["students"]` (no grade at top level — grade is per-student)
---

## 2026-03-18 14:00 - US-004
Session: 69528713-e943-4dd4-95c9-9be982c88137
- Created `src/lib/individual-student-discussion.test.ts` with 37 tests following individual-af-teacher-interaction.test.ts pattern
- Files changed: `src/lib/individual-student-discussion.test.ts` (new, 405 lines)
- All 1475 tests pass across 85 files (no regressions)
- **Learnings for future iterations:**
  - Followed `individual-af-teacher-interaction.test.ts` pattern closely, adapted for student context (no attendance, grade per student)
  - Config integrity tests verify: 1 section titled "Operational Health", 2 questions, unique keys, oh_* prefix
  - Grade validation tests cover per-student grade (11/12 valid, 10/"eleven" invalid) — unlike group discussion where grade is top-level
  - Strict mode additionally requires: name (non-empty string), grade, and all 2 questions answered per student
  - Lenient mode allows missing name, missing grade, and partial questions — same pattern as teacher interaction
  - `buildCompleteStudent()` helper takes id, name, grade params for flexible test data construction
---

## 2026-03-18 14:02 - US-005
Session: 63504e9a-e1e8-446d-b09d-837d3a5ce2fc
- Created `src/lib/student-utils.ts` with `Student` interface and `getStudentDisplayName()` function
- Created `src/lib/student-utils.test.ts` with 3 tests covering all fallback paths
- Files changed: `src/lib/student-utils.ts` (new), `src/lib/student-utils.test.ts` (new)
- All 1478 tests pass across 86 files (no regressions)
- **Learnings for future iterations:**
  - Copied `teacher-utils.ts` pattern exactly — same structure with adapted fields
  - Student interface: `{ id, full_name, student_id, grade }` (teacher has `{ id, email, full_name }`)
  - Fallback chain: `full_name?.trim()` -> `student_id` -> `Student #${id}` (teacher uses `full_name || email`)
---

## 2026-03-18 14:05 - US-006
Session: 30b369dc-8852-466d-8417-789bf52edf2d
- Created `src/app/api/pm/students/route.ts` with GET handler for `/api/pm/students?school_code=XXXXX&grade=11`
- Files changed: `src/app/api/pm/students/route.ts` (new, 70 lines)
- All 1478 tests pass across 86 files (no regressions)
- **Learnings for future iterations:**
  - Copied `src/app/api/pm/teachers/route.ts` auth pattern exactly (session → requireVisitsAccess → school_code parse → school lookup → canAccessVisitSchoolScope)
  - Key difference from teachers: students need `school.id` (not just school_code) for the join chain, and the school query returns `id + region` (teachers only return `region`)
  - Teachers route doesn't 404 on missing school (passes null region); students route returns 404 because `school.id` is required for the query
  - Student query adapted from school page `getStudents()` — simplified to return only `id, full_name, student_id, grade` (no batch/program/status/dates)
  - Grade filter uses `($2::INT IS NULL OR gr.number = $2)` pattern for optional filtering in a single query
  - Dropout exclusion: `(s.status IS NULL OR s.status != 'dropout')` handles both null status (no student record) and non-dropout students
---

## 2026-03-18 14:10 - US-007
Session: 39183ffa-7cdc-4274-91be-cc95864ea972
- Created `src/app/api/pm/students/route.test.ts` with 10 tests following teachers route test pattern
- Files changed: `src/app/api/pm/students/route.test.ts` (new, 166 lines)
- All 1488 tests pass across 87 files (no regressions)
- **Learnings for future iterations:**
  - Followed `src/app/api/pm/teachers/route.test.ts` pattern closely, adapted for students route differences
  - Key difference from teachers: school lookup returns `{id, region}` (teachers only `{region}`), and students route returns 404 for missing school (teachers pass null region)
  - Students route uses `school.id` (not `school_code`) in the final query, so mock chain asserts `[10, null]` or `[10, 11]` for the students query params
  - Grade validation has two error paths: NaN (e.g. "abc") and non-positive integer (e.g. "0") — both return same error message
  - Used `URL` + `searchParams.set()` for building request URLs (cleaner than string interpolation for optional params)
---

## 2026-03-18 14:12 - US-008
Session: 8ec359a7-b81a-48db-ab39-3e8e350aa3bb
- Created `src/components/visits/GroupStudentDiscussionForm.tsx` following PrincipalInteractionForm pattern with grade dropdown
- Files changed: `src/components/visits/GroupStudentDiscussionForm.tsx` (new, 268 lines)
- All 1488 tests pass across 87 files (no regressions), build passes
- **Learnings for future iterations:**
  - Copied PrincipalInteractionForm pattern exactly, added grade dropdown and grade-gated visibility
  - Grade selection uses `VALID_GRADES.includes(data.grade as 11 | 12)` to type-check against the const tuple
  - Questions/progress hidden until grade is selected OR existing question answers detected (handles pre-filled data from API)
  - Section title is dynamic: `"General Check Grade {grade}"` — grade number appended to section title from config
  - Radio names use `group-student-` prefix (not `principal-interaction-`) for unique form grouping
  - `data-testid` uses `group-student-` prefix for all interactive elements
---

## 2026-03-18 14:15 - US-009
Session: a4e0338e-72ff-44a4-ba88-2b16004a3fb4
- Created `src/components/visits/GroupStudentDiscussionForm.test.tsx` with 19 tests following PrincipalInteractionForm.test.tsx pattern
- Files changed: `src/components/visits/GroupStudentDiscussionForm.test.tsx` (new, 329 lines)
- All 1507 tests pass across 88 files (no regressions)
- **Learnings for future iterations:**
  - Followed PrincipalInteractionForm.test.tsx pattern with added grade-specific tests
  - Key difference: grade dropdown gating — questions/progress hidden until grade selected, so most tests need `initialData: { grade: 11 }` or a `selectOptions` call first
  - Test for "shows questions when initialData has existing answers (no grade selected)" covers the `hasQuestionAnswers` fallback path (questions visible even without grade if answers exist)
  - Section title assertion: `"General Check Grade 12"` — dynamic with selected grade
  - Test IDs use `group-student-` prefix throughout (e.g., `group-student-gc_interacted-yes`)
  - `buildCompleteData(grade, answer)` helper takes optional grade param (default 11) — used in disabled mode tests
---

## 2026-03-18 14:20 - US-010
Session: 1a2cd1c1-0912-4b13-ac42-d76fbe04d78c
- Created `src/components/visits/IndividualStudentDiscussionForm.tsx` following IndividualAFTeacherInteractionForm pattern
- Files changed: `src/components/visits/IndividualStudentDiscussionForm.tsx` (new, 432 lines)
- All 1507 tests pass across 88 files (no regressions), build passes
- **Learnings for future iterations:**
  - Adapted IndividualAFTeacherInteractionForm: removed attendance field/radios/badges/gating, added grade filter dropdown + re-fetch on grade change
  - Grade filter is local state (`selectedGrade`), not part of `data` — triggers re-fetch of students from `/api/pm/students?school_code=X&grade=N`
  - `studentsLoading` starts as `false` (not `true` like teacher form) because no fetch happens until grade is selected
  - Student entries have `grade` field (from selectedGrade at time of add) instead of `attendance` field
  - Grade badge uses hardcoded blue colors (`bg-blue-100 text-blue-800`) instead of attendance-specific badge classes
  - No `allStudentsRecorded` check — student count varies by PM discretion
  - `data-testid` prefixes: `student-section-{id}`, `student-header-{id}`, `remove-student-{id}`, `add-student-select`, `student-grade-filter`
  - Student select dropdown only visible when grade is selected AND students loaded AND remaining students exist
---

## 2026-03-18 14:25 - US-011
Session: 4420fc11-2072-4565-8121-0eaf4356cb4f
- Created `src/components/visits/IndividualStudentDiscussionForm.test.tsx` with 23 tests following IndividualAFTeacherInteractionForm.test.tsx pattern
- Files changed: `src/components/visits/IndividualStudentDiscussionForm.test.tsx` (new, 493 lines)
- All 1530 tests pass across 89 files (no regressions)
- **Learnings for future iterations:**
  - Adapted IndividualAFTeacherInteractionForm.test.tsx pattern: removed attendance tests, added grade filter tests
  - Key difference from teacher test: student fetch happens on grade selection (not mount), so tests need `selectGrade(user, "11")` helper before asserting dropdown
  - Grade filter is local state — no fetch until grade selected, so `mockFetch` should not be called before grade selection
  - Loading state tested via never-resolving Promise (`mockFetch = vi.fn().mockReturnValue(new Promise(() => {}))`)
  - No "all students recorded" message to test (unlike teacher form)
  - Student entries use `grade` field (number) instead of `attendance` field (string enum)
  - Progress is `X/2` (2 questions) not `X/13` (13 questions)
  - Grade badge test: `student-grade-badge-{id}` testid with "Grade 11" text
---

## 2026-03-18 14:30 - US-012
Session: d43cd703-56ac-4e5a-b480-65bd4ca1e283
- Wired both student interaction forms into ActionDetailForm.tsx (8 insertion points)
- Files changed: `src/components/visits/ActionDetailForm.tsx` (modified, +138 -40 lines)
- All 1530 tests pass across 89 files (no regressions), build passes
- **Learnings for future iterations:**
  - Followed exact same pattern as principal/teacher sanitize+bootstrap functions — grade handling modeled on classroom observation's grade but stored as number (not string)
  - `sanitizeGroupStudentDiscussionPayload` returns `{ grade, questions }` with `grade: null` as default (not undefined) — consistent with how bootstrap returns safe defaults
  - `sanitizeIndividualStudentDiscussionPayload` is a copy of `sanitizeIndividualTeacherPayload` with `attendance` replaced by `grade` and using `INDIVIDUAL_STUDENT_DISCUSSION_CONFIG.allQuestionKeys`
  - End error message for individual student is distinct from individual teacher: "add at least one student" vs "record all teachers"
  - Both types added to `SAVE_BEFORE_END_TYPES` — triggers save-before-end flow (flush auto-save, save sanitized data, then acquire GPS and end)
  - ACTION_FORM_CONFIGS entries have `fields: []` (empty array) because custom form components handle all rendering — no generic text fields needed
---

## 2026-03-18 14:35 - US-013
Session: 7cdd8c7a-7c65-4715-af8a-7419d53bcc07
- Added `getGroupStudentDiscussionStats()` and `getIndividualStudentDiscussionStats()` to ActionPointList.tsx
- Added card rendering JSX for both student interaction types with data-testid attributes
- Imported `GROUP_STUDENT_DISCUSSION_CONFIG` for question key iteration
- Files changed: `src/components/visits/ActionPointList.tsx` (modified, +96 lines)
- All 1530 tests pass across 89 files (no regressions), build passes
- **Learnings for future iterations:**
  - `getGroupStudentDiscussionStats` follows exact same pattern as `getPrincipalInteractionStats` — iterates `allQuestionKeys`, counts boolean answers, returns null when both grade is null and answeredCount is 0
  - `getIndividualStudentDiscussionStats` follows `getIndividualTeacherInteractionStats` pattern (simpler — just returns `studentCount`, no attendance breakdown)
  - Card rendering: group shows `Grade: N` + `X/4 (Y%)` progress; individual shows `Students: N`
  - `data-testid` patterns: `group-student-stats-{id}`, `individual-student-stats-{id}`
---

## 2026-03-18 14:40 - US-014
Session: 5deefbbb-97a3-4575-8877-c2f6a4245762
- Enabled `group_student_discussion` and `individual_student_discussion` in ActionTypePickerModal enabled condition
- Added 4 new tests (selectable + submit for each type), updated disabled count test from 6 to 4
- Files changed: `src/components/visits/ActionTypePickerModal.tsx` (1 line), `src/components/visits/ActionTypePickerModal.test.tsx` (+62 lines)
- All 1534 tests pass across 89 files (no regressions), build passes
- **Learnings for future iterations:**
  - Simple one-line change: just add `|| actionType === "group_student_discussion" || actionType === "individual_student_discussion"` to the enabled condition on line 47
  - Test pattern for new enabled types: 2 tests per type (radio not disabled + submit flow), plus update disabled count test
  - Now 6 enabled types, 4 disabled types in the picker modal
---

## 2026-03-18 14:45 - US-015
Session: 5bcccb17-53cf-4756-b20b-ee061fc8f38b
- Changed `group_student_discussion` label from "Group Student Discussion" to "Student Interaction" in `src/lib/visit-actions.ts`
- Changed `individual_student_discussion` label from "Individual Student Discussion" to "Individual Student Interaction" in `src/lib/visit-actions.ts`
- Updated label assertions in `src/components/visits/ActionTypePickerModal.test.tsx` (4 getByLabelText calls)
- Files changed: `src/lib/visit-actions.ts`, `src/components/visits/ActionTypePickerModal.test.tsx`
- All 1534 tests pass across 89 files (no regressions)
- **Learnings for future iterations:**
  - Labels in `ACTION_TYPES` map are used as radio button labels in ActionTypePickerModal — test assertions use `getByLabelText()` with these exact strings
  - No other test files referenced the old labels — only ActionTypePickerModal.test.tsx needed updating
---

## 2026-03-18 14:50 - US-016
Session: e25e7b0e-cf7d-4fbb-a6f6-b31ed0c0ea8b
- Added PATCH route validation for `group_student_discussion` and `individual_student_discussion` action types
- Imported validation functions from both student libs
- Added 2 validation blocks after existing `principal_interaction` block following same pattern
- Files changed: `src/app/api/pm/visits/[id]/actions/[actionId]/route.ts` (modified, +30 lines)
- All 1534 tests pass across 89 files (no regressions)
- **Learnings for future iterations:**
  - Exact same pattern as all other action types: `if (action_type === "X") { validate lenient/strict based on status; return 422 if invalid }`
  - Error messages use descriptive type name: "Invalid group student discussion data" / "Invalid individual student discussion data"
  - Imports added alphabetically between existing imports
---

## 2026-03-18 14:55 - US-017
Session: d10334b9-858a-4119-a4bd-43cee06822c0
- Added END route validation for `group_student_discussion` and `individual_student_discussion` action types
- Added `groupStudentDiscussionValidationError()` and `individualStudentDiscussionValidationError()` functions
- Invocations added in both main path and concurrent fallback path
- Imported strict validation functions from both student libs
- Files changed: `src/app/api/pm/visits/[id]/actions/[actionId]/end/route.ts` (modified, +49 lines)
- All 1534 tests pass across 89 files (no regressions)
- **Learnings for future iterations:**
  - Exact same pattern as `principalInteractionValidationError()` — type check → validate strict → return apiError 422 if invalid
  - Both invocations placed after principal interaction check, before `allTeachersRecordedError` (consistent ordering)
  - No 'all students recorded' DB check needed (unlike teacher END which checks `user_permission` table)
  - Concurrent fallback path mirrors main path exactly — same validation functions, same ordering
---

## 2026-03-18 15:00 - US-018
Session: 33b0c373-b18b-4905-9cc4-f434a5ca2908
- Added two new sequential checks in visit completion route after principal_interaction check
- Check 1: At least one completed `group_student_discussion` action required
- Check 2: At least one completed `individual_student_discussion` action required
- Updated 3 existing tests to extend mock chains with 2 extra `.mockResolvedValueOnce()` calls (7→9 total queries)
- Renamed "all 4 action types" test to "all 6 action types"
- Updated mock call index for UPDATE query assertion (calls[6] → calls[8])
- Files changed: `src/app/api/pm/visits/[id]/complete/route.ts` (+34 lines), `src/app/api/pm/visits/[id]/complete/route.test.ts` (+10 lines)
- All 1534 tests pass across 89 files (no regressions)
- **Learnings for future iterations:**
  - Exact same pattern as AF team/individual teacher/principal checks — query for completed action with LIMIT 1, 422 if empty
  - Short-circuit order is now: classroom → AF team → individual teacher → principal → group student → individual student
  - When adding new action type checks to complete route, ALL tests that reach the UPDATE query need their mock chains extended (3 tests in this case)
  - Mock call index for the UPDATE query shifts by the number of new checks added (6→8 with 2 new checks)
---
