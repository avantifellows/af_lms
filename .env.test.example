# Local Postgres credentials for the test DB
# The test framework creates/drops a database called "af_lms_test"
TEST_DB_USER=postgres
TEST_DB_PASSWORD=postgres

# Source database to dump from (your local dev DB)
# Used by: pg_dump -f e2e/fixtures/db-dump.sql $SOURCE_DB_NAME
SOURCE_DB_NAME=af_lms_dev
