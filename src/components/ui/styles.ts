/** Shared base styling for form controls (Input, Select, RemarkField textarea). */
export const baseInputClasses =
  "rounded-lg border-2 border-border px-3 py-2.5 text-sm focus:border-accent focus:outline-none focus:ring-2 focus:ring-accent/20 disabled:cursor-not-allowed disabled:bg-bg-card-alt transition-colors";

/** Minimum touch target — 48px (Google/Apple standard) */
export const touchTarget = "min-h-[48px]";
